[package]
name = "wayland-session-supervisor"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Checkpoint and restore supervised Wayland sessions"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
